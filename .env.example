# Optional: point at a real backend (e.g. https://your-api.example.com).
# If unset, or if the API health check fails, the app starts in Demo Mode
# (Mock Service Worker intercepts requests and serves seeded data).
VITE_API_URL=

# Force demo mode regardless of API availability. Useful for the public deploy.
# VITE_DEMO_MODE=true
